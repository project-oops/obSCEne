# 2026-08-30 - the suite ran to completion on a real console


```text
OBS|meta|1|28|521
OBS|tally|218|74|189|40
obscene: suite complete
```

**Evidence:** [`data/hardware/ps5-full.txt`](../../data/hardware/ps5-full.txt) - 14,665 records,
one binary, every line.
**Build:** `GEN=4`, `HARDWARE=1`, the ten crasher libraries excluded.

The first time anything this project built reached the end of its own suite on hardware rather
than on an emulator. The census reached 10,243 symbols, which bounded - for the first time from
measurement rather than from a corpus - what a title of this shape can actually see.

Getting here needed the package to install, the title to launch, and ten libraries whose load
ends the process to be found one at a time. Those ten are
[`data/hardware/crashers.txt`](../../data/hardware/crashers.txt), and they cost a crash and a
reboot each.

---

