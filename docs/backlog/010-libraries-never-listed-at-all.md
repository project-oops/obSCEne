# 4. Libraries never listed at all


`libSceRtc`, `libSceFios2`, `libSceAvPlayer`, `libSceNgs2`, `libSceVideodec`,
`libSceAjm`, `libSceMouse`, `libSceKeyboard`, `libSceAppContent`, `libSceNpManager`,
`libSceScreenShot`, `libSceRazorCpu`.

Each is one group in `data/surface.txt` once its names are confident. Cheap, and
the census is the right place for them - a wrong name here is a harmless false
negative (D014).

