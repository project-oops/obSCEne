# 9. Not under version control


The repository has `.gitignore`, `.gitattributes` and a `.github` directory, and no
`.git`. Roughly seventy-five source files with no history, including several decisions
that were made, reversed on evidence, and remade - `STT_FUNC` went in, came out as a
regression, and went back in once the "regression" turned out to be the report finally
appearing (D039).

That reversal is recorded here in prose. The diff that would show it does not exist.

