# 6b. `pretty` does not render the responsiveness records


Small, and it matters more than its size. `007-responsive` emits one record per symbol
saying whether the function reads its arguments, and the rendered report shows only the
section verdict. The per-symbol detail - which eight of twenty-seven are implemented - is
in the stream and invisible to anyone reading the pretty output, which is the form most
people will read.

